services:
  - type: web
    name: scanit-ivion-quality-report-parser
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: python main.py
